{
  "name": "sklad",
  "version": "1.0.0",
  "description": "Учёт остатков на складе — мобильное веб-приложение без сборки",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
